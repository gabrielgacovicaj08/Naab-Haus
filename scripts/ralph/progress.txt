# Ralph Progress Log
Started: Tue Mar  3 15:47:03 CST 2026
---

## Codebase Patterns
- Project root: `/c/Users/gabri/desktop/naabhaus` (React + Vite, ESM)
- CSS vars defined in `src/index.css` under `:root`; fonts loaded via `<link>` in `index.html`
- Lint: `npm run lint` (ESLint flat config, no TypeScript — `.jsx` files only)
- Branch: `ralph/sardegna-landing` (created from `main`)
- **ESLint `react-refresh/only-export-components` rule**: Component files (`.jsx`) must ONLY export React components. Hooks and context instances must go in separate files (`.js` preferred for non-components). Pattern: `context/languageContext.js` (context instance) + `context/LanguageContext.jsx` (provider component) + `hooks/useTranslation.js` (hook).
- Translations: `src/translations.js` exports `{ en: {...}, it: {...} }`. Use `useTranslation()` hook which returns `{ t(keyPath), lang, toggleLang }`. Key paths are dot-separated strings like `'hero.headline'`.
- Language persistence: stored in `localStorage` under key `'naabhaus-lang'`.

## 2026-03-04 - US-001
- Implemented foundational design tokens and global styles
- Files changed:
  - `index.html`: added Google Fonts preconnect + stylesheet link (Playfair Display + Inter)
  - `src/index.css`: replaced Vite defaults with CSS variables (color palette), global reset (box-sizing, font-smoothing, scroll-behavior: smooth), base typography
  - `src/App.jsx`: replaced Vite boilerplate with minimal `<main><p>NaabHaus</p></main>`
  - `src/App.css`: cleared Vite boilerplate
- **Learnings for future iterations:**
  - All color/font tokens are in `src/index.css` `:root` — reference via `var(--color-*)` and `var(--font-*)`
  - `--section-padding: 6rem 2rem` defined for consistent section spacing
  - ESLint `no-unused-vars` rule ignores vars matching `^[A-Z_]` (uppercase)
  - No TypeScript — plain `.jsx` files throughout
---

## 2026-03-04 - US-003
- Implemented sticky Navbar with transparent→frosted scroll transition and mobile hamburger menu
- Files changed:
  - `src/components/Navbar.jsx`: Navbar component with scroll-aware class, smooth-scroll links, hamburger toggle, EN/IT lang button
  - `src/components/Navbar.css`: Fixed positioning, transparent→frosted glass on scroll, responsive hamburger menu, desktop/mobile layouts
  - `src/App.jsx`: Replaced inline nav with `<Navbar />`, added section stubs with IDs (home, features, gallery, experience, location) for scroll targets
- **Learnings for future iterations:**
  - Components live in `src/components/`; each component has its own `.css` file imported inside the `.jsx`
  - Navbar uses `window.scrollY > 60` to add `.navbar--scrolled` class for the frosted-glass effect
  - Mobile hamburger at ≤768px: links panel is `position: absolute; top: 4rem` (equals navbar height) so it drops below cleanly
  - Section IDs must match the keys in NAV_LINKS in Navbar.jsx (`home`, `features`, `gallery`, `experience`, `location`)
  - The `<main>` sections need `id` attributes for smooth-scroll to work; hero section needs extra top padding (8rem) to account for fixed navbar height
---

## 2026-03-04 - US-004
- Implemented full-viewport Hero section with Unsplash background, dark overlay, staggered fade-in animations
- Files changed:
  - `src/components/Hero.jsx`: Hero component using `useTranslation` for headline/subheadline/CTA, BOOKING_URL const for `href`
  - `src/components/Hero.css`: 100vh hero, `background-image` via inline style, overlay, staggered `fadeUp` keyframe animations (0.3s / 0.6s / 0.9s delay), subtle `heroScale` on bg image
  - `src/App.jsx`: replaced inline `#home` section with `<Hero />` import
- **Learnings for future iterations:**
  - Hero background image applied via inline `style={{ backgroundImage }}` on a positioned child div (not on the section itself) to allow the overlay div to layer cleanly
  - Stagger animation: set `opacity: 0` + `transform: translateY(24px)` on element, then `animation: fadeUp 0.8s ease forwards` with incrementing delays; `forwards` fill-mode keeps final state
  - The `heroScale` subtle zoom-out on load (scale 1.05 → 1) adds luxury feel without JS
  - `clamp()` used for fluid font sizes: `clamp(2.25rem, 5vw, 4rem)` for headline
---

## 2026-03-04 - US-005
- Implemented Features/highlights section with 6 feature cards, Intersection Observer fade-in, and responsive grid
- Files changed:
  - `src/components/Features.jsx`: reads `features.items` array from translations, renders 6 cards with emoji icon/title/desc, uses `useEffect` + `IntersectionObserver` for staggered fade-in
  - `src/components/Features.css`: white card layout on sandy-white bg, `grid-template-columns: repeat(3,1fr)` → 2 cols at 900px → 1 col at 540px
  - `src/App.jsx`: replaced stub `#features` section with `<Features />`
- **Learnings for future iterations:**
  - `t('features.items')` returns an array — translations.js supports array values, iterate with `.map()`
  - Intersection Observer pattern: attach to `cardRefs.current[i]`, add CSS class on intersect, then `observer.disconnect()` so animation runs once; return cleanup in `useEffect`
  - Stagger effect achieved purely via `transitionDelay` set inline before adding the visible class — no JS animation library needed
---

## 2026-03-04 - US-002
- Implemented LanguageContext, useTranslation hook, and EN/IT translations
- Files changed:
  - `src/translations.js`: all EN and IT strings for every section (nav, hero, features, gallery, experience, location, CTA, footer)
  - `src/context/languageContext.js`: bare `createContext` instance (non-component file)
  - `src/context/LanguageContext.jsx`: `LanguageProvider` component with `lang` state + localStorage persistence
  - `src/hooks/useTranslation.js`: `useTranslation()` hook returning `{ t, lang, toggleLang }`
  - `src/App.jsx`: wrapped with `LanguageProvider`, added minimal nav with EN/IT toggle button, uses `t()` for hero text
- **Learnings for future iterations:**
  - `react-refresh/only-export-components` requires splitting: context instance in `.js`, provider component in `.jsx`, hook in separate `.js`
  - `t('section.key')` resolves dot-separated paths into the translations object
  - Toggle shows the OTHER language label (when lang=en, button shows "IT")
---
